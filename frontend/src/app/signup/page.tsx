import SignupNotice from '@/features/auth/components/SignupNotice';

export const metadata = {
    title: 'Sign Up - Prodify CRM',
};

export default function SignupPage() {
    return <SignupNotice />;
}
